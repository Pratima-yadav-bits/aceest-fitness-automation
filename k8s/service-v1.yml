apiVersion: v1
kind: Service
metadata:
  name: aceest-service-v1
spec:
  type: NodePort
  selector:
    app: aceest
    version: v1
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30001