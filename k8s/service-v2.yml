apiVersion: v1
kind: Service
metadata:
  name: aceest-service-v2
spec:
  type: NodePort
  selector:
    app: aceest
    version: v2
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30002